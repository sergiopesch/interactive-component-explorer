# Optional fallback credentials for server-side inference routes.
# Local browser CV/TTS paths work without these in supported browsers.
HF_TOKEN=hf_your_token_here
# Optional alias used in some deployments (e.g. Vercel)
HF_VARIABLE=hf_your_token_here
